import React from "react";

const Title = ({ name }) => {
  return (
    <>
      <div className="text-white text-center font-bold text-lg px-4 mt-2">{name}</div>
    </>
  );
};

export default Title;
